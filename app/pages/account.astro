---
export const prerender = false;
import AccountPage from '../components/AccountPage.astro';
import { getSessionMerchant } from '../lib/auth';

const merchant = await getSessionMerchant(Astro.cookies);
if (!merchant) return Astro.redirect('/login');
---

<AccountPage lang="en" currentPath="/account" merchant={merchant} />
